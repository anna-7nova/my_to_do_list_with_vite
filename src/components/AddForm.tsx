import React from 'react';
import { Button } from './Button';

export const AddForm = () => {
    return (
        <div>
            <input />
            <Button title='+' />
        </div>
    );
}

